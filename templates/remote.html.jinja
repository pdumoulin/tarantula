<head>
    <title>Remote</title>
    <link rel="stylesheet" type="text/css" href="{{url_for('static', path='/styles/remote.css')}}">
    <script src="{{url_for('static', path='/scripts/remote.js')}}"></script>
    {% include 'favicon.html.jinja' %}
</head>

<body>
    <div class="container">
        {% for button in buttons %}
        <button id="{{button.id}}" class="irbutton">{{button.name}}</button>
        {% endfor %}
    </div>
</body>
