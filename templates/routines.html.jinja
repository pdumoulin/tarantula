<head>
    <title>{{name}}</title>
    <link rel="stylesheet" type="text/css" href="{{url_for('static', path='/styles/routines.css')}}">
    {% include 'favicon.html.jinja' %}
</head>

<body>
    <div>
        {% if success %}
            <p>✅</p>
        {% else %}
            <p>❌</p>
        {% endif %}
    </div>
</body>
